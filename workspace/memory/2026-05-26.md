# 2026-05-26

- Corrigido Telegram em tópicos/grupos: mensagens enviadas por Hugo pelo `from.id` master agora são tratadas como master e respostas usam `message_thread_id`.
- Corrigidos erros de build TypeScript em `context-builder`, `lareport` e `webhook`; `npm run build` passou.

- Corrigido Telegram em tópicos: fila agora é separada por `message_thread_id`, `sendChatAction` usa o tópico e mensagem de erro volta no mesmo tópico.
- Corrigido polling do Telegram: updates agora são despachados sem bloquear o loop enquanto a LLM responde, evitando travar novos tópicos/mensagens.
- Implementados 4 heartbeat jobs em `HEARTBEAT_DRY_RUN=true`: inadimplência diária, alunos sumidos, renovações próximas e aniversariantes do dia; relatórios consolidados vão apenas para o Telegram do master.
- Ajustado destino dos heartbeats: relatórios agora leem `memory/heartbeat-telegram-target.json`; quando Hugo envia mensagem em um tópico do Telegram, esse tópico fica salvo como destino dos próximos relatórios.
