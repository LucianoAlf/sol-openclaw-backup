# Status — Sol Caixa V3 — 2026-08-21

## Veredito

```text
Canario V3 em producao: ativo
Entrada: fail-closed confirmado
Saida: fail-closed confirmado
Correcao de movimento: fail-closed confirmado
Estorno: fail-closed confirmado
Writer de preview financeiro: endurecido
STRICT=1: bloqueado
Gate final: aberto ate caso vivo persistido
```

## Confirmado

- `sol_caixa_lancar_recebimento` chama `sol_caixa_v3_validar_approval_v1`.
- `sol_caixa_lancar_saida` chama `sol_caixa_v3_validar_approval_v1`.
- `sol_caixa_corrigir_movimento_v1` chama `sol_caixa_v3_validar_approval_v1`.
- `sol_caixa_estornar_movimento_v1` chama `sol_caixa_v3_validar_approval_v1`.
- Validator V3:
  - normaliza `lancar_recebimento -> entrada`;
  - normaliza `lancar_saida -> saida`;
  - valida correcao e estorno;
  - exige valor, forma e categoria;
  - bloqueia `NULL` e `unknown`;
  - valida grupo oficial;
  - valida ator pelo helper canonico;
  - confere hash, unidade, approval, operacao e prazo.
- Consumo unico protegido por `sol_caixa_v3_approval_consumos_v1`, RLS e `PRIMARY KEY (approval_id)`.
- RPC legada `sol_caixa_corrigir_forma_recebimento` nao esta executavel por `sol_acesso_restrito`.
- `anon`, `authenticated` e `sol_caixa_readonly` seguem sem acesso as mutadoras.

## Pendente antes de STRICT=1

1. Observar caso vivo persistido:
   `preview -> pode -> approval -> consumo -> mutacao financeira`.
2. Conferir o registro em `sol_caixa_v3_approval_consumos_v1`.
3. Conferir o movimento financeiro correspondente.
4. Rodar smoke final nos 3 grupos.

## Fora do escopo atual

- `sol_caixa_abrir`.
- `sol_caixa_fechar`.
- Substituicao completa do parser antigo pelo classificador deterministico.

## Relatorios principais

- `reports/gate-v3-production-ledger-e2e-2026-08-21.json`
- `reports/gate-v3-failclosed-approval-2026-08-21.json`
- `reports/gate-v3-validator-correcao-estorno-2026-08-21.json`
